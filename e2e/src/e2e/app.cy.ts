describe('e-calendar', () => {
  // beforeEach(() => cy.visit('/'));
  // it('should display My Calendar title', () => {
  //   cy.get('[data-cy="title"]').should('have.text', 'My Calendar');
  // });
});
